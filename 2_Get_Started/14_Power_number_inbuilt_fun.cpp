#include<iostream>
#include<math.h>
using namespace std;
int main(){
    double base,exp;
    cout<<"Enter the base number:";
    cin>>base;
    cout<<"Ente the exponential:";
    cin>>exp;
    cout<<pow(base,exp);
}