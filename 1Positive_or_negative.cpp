#include<iostream>
using namespace std;
int main(){
    int a,b,c;
    cout<<"Enter the number:";
    cin>>b;
    if(b==0)
    cout<<"Zero"; else(b>0)? cout<<"positive":cout<<"Negative"; 
    return 0;
}